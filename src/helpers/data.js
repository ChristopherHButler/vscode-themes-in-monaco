

export const sampleCode = `
import React from 'react';
import ReactDOM from 'react-dom';

export const Themes = {
  DARK_PLUS: {
    value: 'dark+',
    display: 'Dark+ (default dark)',
  },
  LIGHT_PLUS: {
    value: 'light+',
    display: 'Light+ (default light)',
  },
};


const App = () => {

  function test() {
    return;
  }
  test();

  var abc = 123;
  abc++;
  console.log(abc);
};
`;